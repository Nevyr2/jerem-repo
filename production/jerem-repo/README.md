# jerem-repo
jeu java

Got :

    ~ Ball move forward/jump
    ~ obstacle static
    ~ If ball crash -> die

To do :

     ~ map moving
     ~ more obstacle
     ~ check crash
     ~ design ball & map
